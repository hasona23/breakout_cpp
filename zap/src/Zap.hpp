#pragma once


#include <iostream>
#include <string>
#include <format>

#include "raylib.h"
#include "Entities.hpp"
#include "GameCore.hpp"	

// Ensure raylib is installed and the include path is set correctly in your project settings.
// If raylib is installed in a custom location, update the include path accordingly.
// Example: #include <raylib.h> if installed system-wide, or provide the relative path.